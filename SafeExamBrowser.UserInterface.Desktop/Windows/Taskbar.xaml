<Window xmlns:Windows="clr-namespace:SafeExamBrowser.UserInterface.Desktop.Windows"  x:Class="SafeExamBrowser.UserInterface.Desktop.Windows.Taskbar" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SafeExamBrowser.UserInterface.Desktop.Controls.Taskbar"
        xmlns:s="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d" Title="Taskbar" Height="42" Width="750" WindowStyle="None" Topmost="True" AllowsTransparency="True"
        KeyDown="Window_KeyDown" KeyUp="Window_KeyUp"
        ResizeMode="NoResize" Icon="../Images/SafeExamBrowser.ico" Loaded="Window_Loaded" Unloaded="Window_Unloaded" Background="#00000000">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Templates/Colors.xaml" />
                <ResourceDictionary Source="../Templates/ScrollViewers.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid x:Name="UITaskBar" PreviewKeyDown="Window_KeyDown" Background="{DynamicResource BackgroundBrush}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Template="{StaticResource SmallBarScrollViewer}" PreviewKeyDown="Window_KeyDown">
                <StackPanel x:Name="ApplicationStackPanel" Orientation="Horizontal" />
            </ScrollViewer>

            <StackPanel Grid.Column="1" x:Name="NotificationStackPanel" Orientation="Horizontal" VerticalAlignment="Stretch" PreviewKeyDown="Window_KeyDown">
                <Windows:proctorON x:Name="proctorOnIndicator" 
                       Margin="12,12,13,12" 
                       ToolTip="Remote proctoring is active" 
                       Visibility="Collapsed" />

                <Windows:proctorOFF x:Name="proctorOffIndicator" 
                        Margin="12,12,13,12" 
                        ToolTip="Remote proctoring is inactive" 
                        Visibility="Collapsed" />
            </StackPanel>
            <StackPanel Grid.Column="2" x:Name="SystemControlStackPanel" Orientation="Horizontal" VerticalAlignment="Stretch" PreviewKeyDown="Window_KeyDown"/>
            <local:Clock Grid.Column="3" x:Name="Clock" Foreground="{StaticResource SecondaryTextBrush}" Padding="10,0,10,0" PreviewKeyDown="Window_KeyDown"/>
            <local:QuitButton Grid.Column="4" x:Name="QuitButton" PreviewKeyDown="Window_KeyDown"/>
        </Grid>
    </Grid>
</Window>