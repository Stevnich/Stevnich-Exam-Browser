<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:SebPatcherTool" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="SebPatcherTool.Configurator"
        Title="SEB CheatEngine 2 by Stevnich - Configurator"
        Height="616" Width="537"
        Background="#00000000" ResizeMode="NoResize" FontFamily="Arial Rounded MT Bold" Loaded="Window_Loaded" Icon="/Icon_02_ConfigFile.png">

    <Window.Resources>

        <ResourceDictionary>
            <Style x:Key="UwpButtonActiveStyle" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="#F9F9F9"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="BorderBrush" Value="#E5E5E5"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" SnapsToDevicePixels="true">
                                <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter TargetName="border" Property="Background" Value="#FF0078D7"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="true">
                                    <Setter TargetName="border" Property="Background" Value="#FF0078D7"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- RADIO BUTTON STYLE -->
            <Style x:Key="UwpRadioButtonButtonStyle" TargetType="RadioButton">

                <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ControlBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>

                <Setter Property="Template">
                    <Setter.Value>

                        <ControlTemplate TargetType="RadioButton">

                            <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">

                                <Border.Effect>
                                    <DropShadowEffect x:Name="glowShadow"
                                        BlurRadius="60"
                                        Opacity="0.95"
                                        ShadowDepth="2"

                                        Color="#FF3682D6" Direction="90"/>
                                </Border.Effect>

                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Margin="{TemplateBinding Padding}"/>

                            </Border>

                            <ControlTemplate.Triggers>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ControlHover}"/>
                                </Trigger>

                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ControlPressed}"/>
                                </Trigger>

                                <Trigger Property="IsChecked" Value="True">

                                    <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource AccentBrush}"/>

                                    <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource AccentBrush}"/>

                                    <Setter Property="Foreground" Value="White"/>

                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>

                                                <DoubleAnimation
                                                    Storyboard.TargetName="glowShadow"
                                                    Storyboard.TargetProperty="Opacity"
                                                    To="0.85"
                                                    Duration="0:0:0.12"/>

                                                <DoubleAnimation
                                                    Storyboard.TargetName="glowShadow"
                                                    Storyboard.TargetProperty="BlurRadius"
                                                    To="25"
                                                    Duration="0:0:0.12"/>

                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>

                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>

                                                <DoubleAnimation
                                                    Storyboard.TargetName="glowShadow"
                                                    Storyboard.TargetProperty="Opacity"
                                                    To="0"
                                                    Duration="0:0:0.12"/>

                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>

                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>

                    </Setter.Value>
                </Setter>

            </Style>

            <!-- BUTTON STYLE -->
            <Style x:Key="UwpButtonStyle" TargetType="Button">

                <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ControlBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="6"/>
                <Setter Property="FontSize" Value="14"/>

                <Setter Property="Template">
                    <Setter.Value>

                        <ControlTemplate TargetType="Button">

                            <Border x:Name="border"
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
CornerRadius="4">

                                <ContentPresenter
HorizontalAlignment="Center"
VerticalAlignment="Center"
Margin="{TemplateBinding Padding}"/>

                            </Border>

                            <ControlTemplate.Triggers>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border"
Property="Background"
Value="{DynamicResource ControlHover}"/>
                                </Trigger>

                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border"
Property="Background"
Value="{DynamicResource ControlPressed}"/>
                                </Trigger>

                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border"
Property="Opacity"
Value="0.5"/>
                                </Trigger>

                            </ControlTemplate.Triggers>

                        </ControlTemplate>

                    </Setter.Value>
                </Setter>

            </Style>

            <!-- CHECKBOX STYLE -->
            <Style x:Key="ModernEmbossCheckBoxStyle" TargetType="CheckBox">

                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>

                <Setter Property="Template">
                    <Setter.Value>

                        <ControlTemplate TargetType="CheckBox">

                            <Grid x:Name="templateRoot" Background="Transparent">

                                <StackPanel Orientation="Horizontal">

                                    <Border x:Name="checkBoxBorder"
Width="20"
Height="20"
CornerRadius="4"
Background="{DynamicResource ControlBackground}"
BorderBrush="{DynamicResource ControlBorder}"
BorderThickness="1"
VerticalAlignment="Center">

                                        <Border.Effect>
                                            <DropShadowEffect x:Name="checkGlow"
BlurRadius="5"
Opacity="0"
ShadowDepth="0"
Color="#FF3D88ED" RenderingBias="Quality"/>
                                        </Border.Effect>

                                        <Path x:Name="optionMark"
Data="M 0 3.5 L 3 6.5 L 9 0"
Stroke="White"
StrokeThickness="2"
Opacity="0"
Visibility="Collapsed"
HorizontalAlignment="Center"
VerticalAlignment="Center"
Stretch="Uniform"
Margin="4"/>

                                    </Border>

                                    <ContentPresenter
Margin="10,0,0,0"
VerticalAlignment="Center"/>

                                </StackPanel>

                            </Grid>

                            <ControlTemplate.Triggers>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="checkBoxBorder"
Property="Background"
Value="{DynamicResource ControlHover}"/>

                                    <Setter TargetName="checkBoxBorder"
Property="BorderBrush"
Value="{DynamicResource AccentBrush}"/>
                                </Trigger>

                                <Trigger Property="IsChecked" Value="True">

                                    <Setter TargetName="checkBoxBorder"
Property="Background"
Value="{DynamicResource AccentBrush}"/>

                                    <Setter TargetName="checkBoxBorder"
Property="BorderBrush"
Value="{DynamicResource AccentBrush}"/>

                                    <Setter TargetName="optionMark"
Property="Visibility"
Value="Visible"/>

                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>

                                                <DoubleAnimation
Storyboard.TargetName="checkGlow"
Storyboard.TargetProperty="Opacity"
To="0.85"
Duration="0:0:0.12"/>

                                                <DoubleAnimation
Storyboard.TargetName="checkGlow"
Storyboard.TargetProperty="BlurRadius"
To="20"
Duration="0:0:0.12"/>

                                                <DoubleAnimation
Storyboard.TargetName="optionMark"
Storyboard.TargetProperty="Opacity"
From="0"
To="1"
Duration="0:0:0.1"/>

                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>

                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>

                                                <DoubleAnimation
Storyboard.TargetName="checkGlow"
Storyboard.TargetProperty="Opacity"
To="0"
Duration="0:0:0.12"/>

                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>

                                </Trigger>

                                <Trigger Property="IsEnabled" Value="False">

                                    <Setter TargetName="templateRoot"
Property="Opacity"
Value="0.5"/>

                                    <Setter TargetName="checkBoxBorder"
Property="Background"
Value="{DynamicResource ControlPressed}"/>

                                </Trigger>

                            </ControlTemplate.Triggers>

                        </ControlTemplate>

                    </Setter.Value>
                </Setter>

            </Style>

        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome 
            CaptionHeight="32" 
            ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}"
            GlassFrameThickness="-1"
            CornerRadius="0" />
    </WindowChrome.WindowChrome>

    <Grid Background="{DynamicResource ControlBackground}">
        <Grid VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBlock x:Name="windowTitle" 
                       VerticalAlignment="Center" 
                       Margin="10,0" 
                       Foreground="{DynamicResource SecondaryText}" Text="text" FontFamily="Arial Rounded MT Bold" Padding="0,6,0,0" FontSize="14" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"/>
        </Grid>
        <Grid Margin="0,28,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="25,15">

                    <!-- Section title -->
                    <TextBlock Text="General:"
                       FontSize="20"
                       Foreground="{DynamicResource AccentBrush}"
                       Margin="0,0,0,15" FontWeight="Bold"/>

                    <Grid Margin="20,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="195"/>
                            <ColumnDefinition Width="177"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="45"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Message Display Delay:"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{DynamicResource PrimaryText}"/>

                        <local:numericUpDown Grid.Row="0"
                                     Grid.Column="1"
                                     x:Name="notificationPopupDelay"
                                     Value="500"
                                     MaxValue="5000000"
                                     Margin="0,5,0,5"
                                     MinValue="1"/>

                        <TextBlock Grid.Column="2"
                           Text="milliseconds"
                           VerticalAlignment="Center"
                           Margin="2.7,0,0,0"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryText}"/>

                        <TextBlock Grid.Row="1"
                           Text="Dummy Proctoring:"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{DynamicResource PrimaryText}"/>

                        <UniformGrid Grid.Row="1"
                             Grid.Column="1"
                             Grid.ColumnSpan="2"
                             Columns="3"
                             Margin="0,6,0,5">

                            <RadioButton x:Name="proctorSetupActive"
                                 Content="Active"
                                 IsChecked="True"
                                 Style="{StaticResource UwpRadioButtonButtonStyle}"
                                 Margin="2"
                                 Cursor="Hand"/>

                            <RadioButton x:Name="proctorSetupInactive"
                                 Content="Inactive"
                                 Style="{StaticResource UwpRadioButtonButtonStyle}"
                                 Margin="2"
                                 Cursor="Hand"/>

                            <RadioButton x:Name="proctorSetupNone"
                                 Content="Disabled"
                                 Style="{StaticResource UwpRadioButtonButtonStyle}"
                                 Margin="2"
                                 Cursor="Hand"/>
                        </UniformGrid>
                    </Grid>

                    <Separator Margin="0,10" Background="{DynamicResource ControlBorder}"></Separator>

                    <TextBlock Text="Action Center Settings:"
                       FontSize="20"
                       Foreground="{DynamicResource AccentBrush}"
                       Margin="0,15,0,15" FontWeight="Bold"/>

                    <Grid Margin="20,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="205"/>
                            <ColumnDefinition Width="156"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="40"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Appearance Duration:"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{DynamicResource PrimaryText}"/>

                        <local:numericUpDown Grid.Row="0"
                                     Grid.Column="1"
                                     x:Name="actionCenterPopupDuration"
                                     Value="2000"
                                     Margin="0,5,0,5"
                                     MaxValue="5000000"
                                     MinValue="1"/>

                        <TextBlock Grid.Column="2"
                           Text="milliseconds"
                           VerticalAlignment="Center"
                           Margin="10,0,10,0"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryText}"/>

                        <TextBlock Grid.Row="1"
                           Text="In Transition Duration:"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{DynamicResource PrimaryText}"/>

                        <local:numericUpDown Grid.Row="1"
                                     Grid.Column="1"
                                     x:Name="actionCenterAnimInDur"
                                     Value="600"
                                     Margin="0,5,0,5"
                                     MaxValue="5000000"
                                     MinValue="1"/>

                        <TextBlock Grid.Row="1"
                           Grid.Column="2"
                           Text="milliseconds"
                           VerticalAlignment="Center"
                           Margin="10,0,10,0"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryText}"/>

                        <TextBlock Grid.Row="2"
                           Text="Out Transition Duration:"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{DynamicResource PrimaryText}"/>

                        <local:numericUpDown Grid.Row="2"
                                     Grid.Column="1"
                                     x:Name="actionCenterAnimOutDur"
                                     Value="550"
                                     Margin="0,5,0,5"
                                     MaxValue="5000000"
                                     MinValue="1"/>

                        <TextBlock Grid.Row="2"
                           Grid.Column="2"
                           Text="milliseconds"
                           VerticalAlignment="Center"
                           Margin="10,0,10,0"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryText}"/>
                    </Grid>

                    <Separator Margin="0,10" Background="{DynamicResource ControlBorder}"></Separator>

                    <TextBlock Text="Browser UI Settings:"
                       FontSize="20"
                       Foreground="{DynamicResource AccentBrush}"
                       Margin="0,15,0,15" FontWeight="Bold"/>

                    <Grid Margin="20,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <CheckBox x:Name="chkMakeWebpagesEditable"
                          Style="{StaticResource ModernEmbossCheckBoxStyle}"
                          Content="Make Webpages Editable (Design Mode)"
                          Margin="0,5,0,10" IsChecked="True"/>

                        <CheckBox Grid.Row="1"
                          x:Name="chkOnlyAllowDesignModeDebug"
                          Style="{StaticResource ModernEmbossCheckBoxStyle}"
                          Content="Only Allow Design Mode in Debug Mode"
                          Margin="0,5,0,10"
                          IsEnabled="{Binding ElementName=chkMakeWebpagesEditable, Path=IsChecked}"/>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <!-- Bottom Bar -->
            <Border Grid.Row="1"
            Background="{DynamicResource ControlBackground}"
            BorderBrush="{DynamicResource ControlBorder}"
            BorderThickness="0,1,0,0">

                <Grid Margin="15,0">

                    <Button x:Name="cancelBtn"
                    Content="Cancel"
                    Style="{StaticResource UwpButtonStyle}"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Width="100"
                    Height="32"
                    Click="cancelBtn_Click"
                    Cursor="Hand"/>

                    <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">

                        <Button Content="Apply &amp; Close"
                        Style="{StaticResource UwpButtonStyle}"
                        Width="130"
                        Height="32"
                        Margin="0,0,10,0"
                        Click="Button_Click_1"
                        Cursor="Hand"/>

                        <Button Content="Apply"
                        Style="{StaticResource UwpButtonActiveStyle}"
                        Width="100"
                        Height="32"
                        Click="Button_Click"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="White"
                        Cursor="Hand" BorderBrush="{x:Null}">

                            <Button.Effect>
                                <DropShadowEffect Opacity="0.3"
                                          BlurRadius="10"/>
                            </Button.Effect>

                        </Button>
                    </StackPanel>
                </Grid>

            </Border>

        </Grid>
        <Ellipse HorizontalAlignment="Right" Height="13" VerticalAlignment="Top" Width="13" Fill="#FFB52A2A" Margin="0,6,9,0"/>
    </Grid>
</Window>